<h1>Editing user</h1>

<%= render 'form' %>

<%url = '/beaten/?winner=#{user.id}&loser='
  for singleUser in @users
    button_to singleUser.name, url + singleUser.id.to_s, :method => "get" 
  end%>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
